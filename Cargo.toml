[package]
name = "uis-x1"
version = "0.1.0"
edition = "2024"
description = "UI experiments toward an actor-model UI with TUI and GUI backends"

[dependencies]

[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
